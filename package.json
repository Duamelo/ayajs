{
  "name": "ayajs",
  "version": "1.0.0",
  "description": "librairie javascript de création de forme géométrique",
  "main": "build/aya.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rollup -c rollup.dev.js",
		"build": "rollup -c && npm run uglify",
    "uglify": "uglifyjs --compress --mangle --output build/aya.min.js -- build/aya.js"

  },
  "keywords": [
    "tape",
    "js",
    "node"
  ],
  "author": "emack",
  "license": "ISC",
  "dependencies": {
    "jsdom": "^19.0.0",
    "tape": "^5.5.2"
  }
}
